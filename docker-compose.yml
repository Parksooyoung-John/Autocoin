services:
  trading-bot:
    build: .
    container_name: xrp-semi-auto-bot
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
