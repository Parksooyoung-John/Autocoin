services:
  trading-bot:
    build: .
    container_name: binance-futures-auto-bot
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
